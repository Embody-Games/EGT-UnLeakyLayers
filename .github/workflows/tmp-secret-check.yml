# TEMPORARY. Answers one question: does DISCORD_WEBHOOK_URL reach this repo?
# Posts nothing. Delete once the answer is known.
name: tmp secret check

on:
  push:
    branches: [standardise-release-tooling]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Is DISCORD_WEBHOOK_URL reachable here?
        run: |
          if [ -z "${{ secrets.DISCORD_WEBHOOK_URL }}" ]; then
            echo "EMPTY: this repo cannot see DISCORD_WEBHOOK_URL"
            exit 1
          fi
          echo "PRESENT: this repo can see DISCORD_WEBHOOK_URL"
